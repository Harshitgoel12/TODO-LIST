<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .container{
            min-height: 100vh;
            width: 100vw;
         background-color: aqua;
         display: flex;
         padding-top: 3rem;
         align-items: center;
        flex-direction: column;
        }
        .formcontainer{
            height: 210px;
            width: 500px;
            background-color: brown;
            border-radius: 10px;
             display: flex;
             overflow: hidden;
             justify-content: center;
             align-items: center;
             flex-direction: column;
             padding: 30px;
        }
        input{
            width: 100%;
            height: 40px;
            font-size: 16px;
             font-family: cursive;
             border-radius: 10px ;
             border: none;
             padding-left: 6px;
        }
        textarea{
            width: 100%;
            height: 80px;
            resize: none;
            border-radius: 10px;
            border: none;
            margin-top: 10px;
            font-family: cursive;
            font-size: 16px;
            padding-top: 5px;
        }
       .formcontainer button{
            height: 40px;
            width: 100px;
           background-color: rgb(43, 43, 200);
           color: white;
           border-radius: 10px;
           font-size: 13px;
            position: relative;
            left: 200px;
            top: 10px;
        } 
    </style>
</head>

<body>
    <div class="container">
        <div class="formcontainer">
    <form action="/updatedata/<%= data[0]._id %>" method="post">
        <input type="text" value="<%=data[0].title%>" name="title" placeholder="Enter title">
        <textarea name="discription"  id="" placeholder="Enter the discription">
            <%=data[0].discription%>
        </textarea>
        <button type="submit">Update</button>
    </form>
</div>

</div>
</div>
</body>
</html>